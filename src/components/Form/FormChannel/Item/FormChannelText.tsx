import { Box } from '@components/Box';
import { Typography } from '@components/Typography';

export const FormChannelText = () => {
  return (
    <Box display={'flex'} alignItems={'center'} height={'56px'}>
      <Typography variant={'body2'} children={'SEA'} />
    </Box>
  );
};
